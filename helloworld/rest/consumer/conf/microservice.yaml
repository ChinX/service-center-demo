service: # 微服务配置
  appId: helloword #应用程序唯一标识
  name: RestConsumerDemo #微服务名称
  version: 1.0.0 #微服务版本
provider:
  appId: helloword #provider应用程序唯一标识
  name: RestProviderDemo #provider微服务名称
  versionRule: 1.0.0 #provider服务发现版本规则（支持：1.精确版本匹配 2.后续版本匹配 3.最新版本 4.版本范围）
registry:
  address: http://127.0.0.1:30100 # service-center 注册中心通讯地址